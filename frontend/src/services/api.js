// Importar o novo serviço Supabase
import supabaseApi from './supabaseApi';

// Exportar o serviço Supabase como padrão
// Isso mantém a compatibilidade com o código existente
export default supabaseApi;
